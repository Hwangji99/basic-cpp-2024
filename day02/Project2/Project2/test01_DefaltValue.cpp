// date : 2024-02-22
// file : test01.DelfaltValue.cpp
// desc : 매개변수의 디폴트 값

// 매개변수의 초기값을 준 경우 : 함수 호출 시 입력값을 따로 넣지 않으면 디폴트 값이 나온다.

/*
#include <iostream>

using namespace std;

int Adder(int num1 = 1, int num2 = 2)	// Adder라는 함수 선언과 함께 디폴트 값으로 num1에 1, num2에 2를 넣는다
{
	return num1 + num2;		// 반환값으로 num1 + num2가 나온다
}

int main(void)
{
	cout << Adder() << endl;
	cout << Adder(5) << endl;		// num1에 5를 넣고 num2의 디폴트 값이 합해져 7
	cout << Adder(3, 5) << endl;	// num1에 3을 넣고 num2에 5를 넣어 합해져 8
	return 0;
}
*/