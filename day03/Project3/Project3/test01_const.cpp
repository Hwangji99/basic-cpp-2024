// date : 2024-02-23
// file : test01_const.cpp
// desc : 상수 참조

/*
#include <iostream>

using namespace std;

int main()
{
	const int num = 20;		// 상수를 선언할 때는 바로 초기화를 해야만 한다		// const int(상수)라는 또 다른 자료형이 있다
	int& ref = num;			// num을 상수화를 시켰기 때문에 다른 변수를 참조자로 할 수가 없다
	ref += 10;
	cout << num << endl;
		return 0;
}
*/
// const라는 키워드가 붙으면 상수(리터럴)도 참조할 수 있다.

