// date : 2024-02-23
// file : test13_RacingMain.cpp
// desc : 파일 분할 3번

/*
#include <iostream>

using namespace std;

class AClass {
private:					// 생략해도 됨	// 없어도 다른 곳에선 사용할 수 없음		
	int num;

public:						// 접근제어 지시자 (다 쓸 수 있음)
	AClass(int anum) {		// 생성자 : 초기화 담당			// a(10)의 값이 이리로 온다
		num = anum;
	}
	void AInfo() {
		std::cout << "A::num: " << num << endl;
	}
};

int main()
{
	AClass a(10);		// AClass에 있는 생성자를 호출
	a.AInfo();

	return 0;
}
*/